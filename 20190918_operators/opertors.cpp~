#include<iostream>

int main(void)
{
  int a;
  std::cin >> a;
  if(a%3==0)
    {
      std::cout << a << " SI es divisible por 3";
    }
  else
    {
      std::cout << a << " NO es divisible por 3";
    }

  return 0;
    
}
